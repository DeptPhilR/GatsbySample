# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

variables:
  - group: dev
  
pool:
  vmImage: ubuntu-latest

resources:
  webhooks:
    - webhook: Contentful2          ### Webhook alias
      connection: Contentful2    ### Incoming webhook service connection

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
  displayName: 'npm install and build'
  env:
    CONTENTFUL_ACCESS_TOKEN: $(CONTENTFUL_ACCESS_TOKEN)
    CONTENTFUL_SPACE_ID: $(CONTENTFUL_SPACE_ID)
- task: PublishBuildArtifacts@1
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: 'drop'
    publishLocation: 'Container'
- task: AzureStaticWebApp@0
  inputs:
    app_location: './public'
    api_location: ''
    skip_app_build: true
    azure_static_web_apps_api_token: 'e09fb58967c15956d06c7a11d7439a2327812cabe912637803505d6ffd99dbfb0-733fd833-5816-415f-b23a-ef80875b2e7701026901'